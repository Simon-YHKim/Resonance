{
  "name": "@resonance/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers backend stub — Phase 1 placeholder",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.93.0",
    "@resonance/shared": "workspace:*",
    "hono": "^4.6.13",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9",
    "wrangler": "^3.99.0"
  }
}
